# Example ~/.redish_config for redish
# Format is similar to SSH config: one block per Redis host.

Host analytics-cache
  HostName analytics.redis.internal.company.com
  Port 6379
  Password analyticsPassword

Host dev-redis
  HostName localhost
  Port 6379
  Password devpassword

# You can add as many hosts as you need using the same structure.
# TLS can be set to "yes" if your Redis instance requires TLS.
# Spaces/tabs before each config line are allowed.

# Host <name>
#   HostName <redis-server-address>
#   Port <port>            # Optional, defaults to 6379
#   Password <password>
#   TLS yes                # Optional, defaults to no

# Save this file as ~/.redish_config.